take care of bad input.


in mission3 - if there are codes that don't exist, ask for new?

type of return value of the pipe:
- all good - print the BUFFER_SIZE
- notAllInDB - print the problem and gets the choice and than return it to the child
- else problems - 